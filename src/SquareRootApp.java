public class SquareRootApp {
    public static void main(String[] args) {
double a = Double.parseDouble(args[0]);             //podanie argumentu przy starcie programu
                                                    // przekonwertuje ciąg znaków na typ zmiennoprzecinkowy double
        System.out.println(Math.sqrt(a));
    }
}
